---
description: One-line description of what this skill does and when to use it
---

# Example Skill

## Purpose

Explain the capability this skill adds. The `description` frontmatter field is
required — it is what the agent uses to decide when to invoke the skill.

## Usage

Describe how and when the skill runs. Add any supporting files alongside this
`SKILL.md` inside the `skills/example-skill/` directory.
